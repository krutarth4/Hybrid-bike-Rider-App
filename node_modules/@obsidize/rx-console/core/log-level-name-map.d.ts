export type LogLevelNameConfig = Record<string, number>;
export type CustomLevelNameDelegate = (level: number) => string;
/**
 * Utility to customize numeric level name mappings.
 */
export declare class LogLevelNameMap {
    static readonly main: LogLevelNameMap;
    private readonly namesByLevel;
    private readonly config;
    private readonly customizer;
    constructor();
    get(level: number): string;
    reset(): void;
    update(config: LogLevelNameConfig): void;
}
